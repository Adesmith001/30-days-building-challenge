export function BattleReward({ correct, score, winner, onNext }: { correct: boolean; score: number; winner: string; onNext: () => void }) {
  return <section className={`battle-reward ${correct ? "win" : "loss"}`} aria-live="polite">{correct && <div className="victory-burst" aria-hidden="true">{Array.from({ length: 12 }, (_, index) => <i key={index} />)}</div>}<span className="reward-kicker">{correct ? "⚡ BREAKTHROUGH" : "✕ DEFENCES HOLD"}</span><h2>{winner} WINS</h2><strong>{correct ? `+${score.toLocaleString()} POINTS` : "NO POINTS THIS ROUND"}</strong><button className="primary-button continue-button" onClick={onNext}>CONTINUE <span>→</span></button></section>;
}
